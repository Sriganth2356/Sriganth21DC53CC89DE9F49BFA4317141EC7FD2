def linear_search_product(products, target_product) Indices = []

for 1, product in enumerate(products): If product = target_product:

Indices.append())

return indices

products = ["Apple", "Banana", "Orange". "Apple", "Grapes", "Apple"] target_product = "Apple"

result = linear_search_product(products, target_product) if result: print("The target product (target product)' was found at indices:

(result)")

else:

print("The target product '(target_product)' was not found in the list.")
